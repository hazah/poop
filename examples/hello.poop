-- hello.poop

-- Object Main is used as the program's starting point by invoking
-- the module method on it. We pass the locally defined main object
-- to inform the environment where to begin execution.

create Module
	Main <- main

-- Local object used to start execution. We are assigning a block to main
-- which will be executed when the program begins running

main ->
	put "Hello, POOP!!"